{% extends 'base.html.twig' %}

{% set title_translation_key = 'home.page-title' %}
{% set heading_class = "govuk-heading-xl" %}

{% block metaDescription -%}
    Use this service to complete your travel diary.
    DfT - GOV.UK
{%- endblock %}

{% block content %}
    {% from '@GhostGovUkFrontend/components/macros.html.twig' import startNow, notificationBanner %}
{#    {%- if maintenanceWarningBanner -%}#}
{#        {{- notificationBanner(maintenanceWarningBanner) -}}#}
{#    {%- endif -%}#}

    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
            <p class="govuk-body">{{- 'home.intro' | trans }}</p>

            {{ startNow(path('app_login'), 'home.sign-in' | trans) }}
        </div>
        <div class="govuk-grid-column-one-third">
        </div>
    </div>

    <hr class="govuk-section-break govuk-section-break--l govuk-section-break--visible">

    <h2 class="govuk-heading-m">{{ 'home.session.title' | trans }}</h2>
    <p class="govuk-body">
        {{- 'home.session.copy' | trans }}
    </p>
{% endblock %}
